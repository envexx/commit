import { AdaptiveHome } from '@/components/work/AdaptiveHome'

// Kept for old links (/about, shared notes): the adaptive home now owns the
// list experience, so this route renders exactly what / renders.
export default function MilestonesPage() {
  return <AdaptiveHome />
}
